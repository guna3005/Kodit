const baseURL = "http://localhost:8080/api/v1";


const userLogin = `${baseURL}/login`;
const userRegistration = `${baseURL}/register`

const ApiConstants = {
    userLogin,
    userRegistration
}


export default ApiConstants;